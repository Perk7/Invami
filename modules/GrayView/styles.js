import { StyleSheet } from "react-native"
import { bgGray } from "../../constants"

export default StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: bgGray,
    },

    contentView: {
      justifyContent: 'center',
    },
})