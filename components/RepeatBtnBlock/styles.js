import { StyleSheet } from "react-native"

export default StyleSheet.create({  
  repeatSendBtn: {
    paddingTop: 15,
    paddingLeft: 20,
    flexDirection: 'row'
  },

  repeatBtnLabel: {
    color: '#FFF',
    fontSize: 15,
  }
})