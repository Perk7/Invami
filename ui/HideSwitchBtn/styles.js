import { StyleSheet } from "react-native"

export default StyleSheet.create({
    button: {
        alignItems: 'center',
        justifyContent: 'center',
    },

    buttonImage: {
        width: 30,
        height: 30
    }
})